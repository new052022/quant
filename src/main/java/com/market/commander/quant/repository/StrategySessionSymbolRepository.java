package com.market.commander.quant.repository;

import com.market.commander.quant.entities.StrategySessionSymbol;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StrategySessionSymbolRepository extends JpaRepository<StrategySessionSymbol, Long> {
}
